apiVersion: v1
kind: Service
metadata:
  name: ru-study
  namespace: ru-study
  labels:
    app.kubernetes.io/name: ru-study
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ru-study
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
