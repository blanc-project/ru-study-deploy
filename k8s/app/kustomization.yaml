apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: ghcr.io/blanc-project/ru-study-app # not live until the source repo actually transfers there
    newTag: 48c943df3a563cbdce9da2cff224354ef213d06a
