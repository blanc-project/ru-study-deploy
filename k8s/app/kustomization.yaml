apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: ghcr.io/blanc-project/ru-study-app # not live until the source repo actually transfers there
    newTag: 76bee982ac2492d115f314c6cbcdb28c4dac3c7d
